<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hotro_vn">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
   <uses-feature
    android:glEsVersion="0x00020000"
    android:required="true"/>


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HOTRO_VN"
        android:windowSoftInputMode="adjustResize">
        <activity android:name=".Activity.Maps.GoogleMaps_Activity"/>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/API_KEY" />

        <activity android:name=".Activity.Thongtincuutro_manhthuongquan.Thongtincuutro_manhthuongquan" />
        <activity android:name=".Activity.Bangtincuutro_manhthuongquan.Bangtincuutro" />
        <activity android:name=".Activity.News.News_Activity" />
        <activity android:name=".Activity.Thongtincuutro.Thongtincuutro_Activity" />
        <activity android:name=".Activity.Chiendichcuutro.Infor_Activity" />
        <activity android:name=".Activity.Phone.Phone2_Activity" />
        <activity android:name=".Activity.Phone.PhoneActivity" />
        <activity android:name=".Activity.Add_news.Add_new" />
        <activity android:name=".Activity.Home.Home">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>